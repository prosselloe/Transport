name: myapp
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  provider: ^6.0.0
  go_router: ^17.0.0
  http: ^1.0.0
  google_maps_flutter: ^2.0.0
  google_fonts: ^8.0.0
  url_launcher: ^6.3.2
  shared_preferences: ^2.0.0
  flutter_native_splash: ^2.0.0
  flexible_polyline_dart: ^1.0.0
  flutter_map: ^8.2.2
  latlong2: ^0.9.1
  geolocator: ^14.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
